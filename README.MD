# :page_facing_up: Market Gateway

Market Gateway is a FastAPI service that implements user authentication based on JWT tokens (access + refresh) and subsequent routing between services.

## :question: Usage Instruction

Add .env (check .env.example) file containing environment variables that specify the keys, algorithm, and database connection.